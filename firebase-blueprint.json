{
  "entities": {
    "userKey": {
      "title": "UserVaultKey",
      "description": "Client-side AES-GCM 256-bit encryption key securely escrowed in a separate document for the authenticated user",
      "type": "object",
      "properties": {
        "keyJwk": {
          "type": "string",
          "description": "JWK serialized CryptoKey representation"
        },
        "algorithm": {
          "type": "string",
          "description": "Cryptographic algorithm (AES-GCM-256)"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of key generation"
        },
        "userId": {
          "type": "string",
          "description": "Firebase Auth UID of the owner"
        }
      },
      "required": ["keyJwk", "algorithm", "createdAt", "userId"]
    },
    "interaction": {
      "title": "JournalInteraction",
      "description": "User journal reflection and Gemini dialogue record with client-side AES-GCM encryption",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the reflection entry (plaintext or ciphertext)"
        },
        "content": {
          "type": "string",
          "description": "User inquiry or reflection text. Stored as AES-GCM ciphertext when isEncrypted is true"
        },
        "mode": {
          "type": "string",
          "description": "Reflection mode: reflect, summarize, or brainstorm"
        },
        "mood": {
          "type": "string",
          "description": "User disposition or emotional state"
        },
        "geminiResponse": {
          "type": "string",
          "description": "The synthesis from Gemini. Stored as AES-GCM ciphertext when isEncrypted is true"
        },
        "modelUsed": {
          "type": "string",
          "description": "The Gemini model that synthesized the response"
        },
        "isEncrypted": {
          "type": "boolean",
          "description": "Whether content and dialectic dialogue are encrypted with client-side AES-GCM"
        },
        "encryptionIv": {
          "type": "string",
          "description": "Base64 initialization vector for AES-GCM ciphertext"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last update"
        },
        "userId": {
          "type": "string",
          "description": "Firebase Auth UID of the owner"
        }
      },
      "required": ["content", "geminiResponse", "userId", "createdAt"]
    },
    "weeklySummary": {
      "title": "WeeklyReflectionSummary",
      "description": "Weekly trend and pattern synthesis across 7 days of reflections, encrypted with AES-GCM",
      "type": "object",
      "properties": {
        "summaryCiphertext": {
          "type": "string",
          "description": "AES-GCM encrypted synthesis text"
        },
        "summaryIv": {
          "type": "string",
          "description": "Base64 initialization vector for summaryCiphertext"
        },
        "themesCiphertext": {
          "type": "string",
          "description": "AES-GCM encrypted JSON array of recurring themes"
        },
        "themesIv": {
          "type": "string",
          "description": "Base64 initialization vector for themesCiphertext"
        },
        "startDate": {
          "type": "string",
          "description": "Start ISO date of the analyzed 7-day window"
        },
        "endDate": {
          "type": "string",
          "description": "End ISO date of the analyzed 7-day window"
        },
        "entryCount": {
          "type": "number",
          "description": "Number of user entries synthesized (capped at 10)"
        },
        "modelUsed": {
          "type": "string",
          "description": "Gemini model used for synthesis"
        },
        "userId": {
          "type": "string",
          "description": "Firebase Auth UID of the owner"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of creation"
        },
        "isEncrypted": {
          "type": "boolean",
          "description": "True when encrypted with client-side AES-GCM"
        }
      },
      "required": ["summaryCiphertext", "summaryIv", "userId", "createdAt", "startDate", "endDate"]
    }
  },
  "firestore": {
    "/users/{userId}/interactions/{interactionId}": {
      "schema": "interaction",
      "description": "Private owner-isolated reflections and dialectic history"
    },
    "/users/{userId}/keys/{keyId}": {
      "schema": "userKey",
      "description": "Private owner-isolated vault encryption keys separated from interaction documents"
    },
    "/users/{userId}/weekly_summaries/{summaryId}": {
      "schema": "weeklySummary",
      "description": "Private owner-isolated encrypted weekly pattern syntheses and mood trends"
    }
  }
}
